# Example env file. Copy to `.env` or use environment-specific files like `.env.development` / `.env.production`.
# Vite requires custom env vars to start with `VITE_`.

# Development API base
VITE_API_BASE=https://delivery-app-backend-dev.up.railway.app

# Production API base (set this in Vercel or use `.env.production` locally)
# VITE_API_BASE=https://delivery-app-backend-production-64f2.up.railway.app
